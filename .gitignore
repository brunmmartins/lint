# Rust build output
/target/

# Local project planning, agent tooling, and generated delivery state
/.agents/
/.claude/
/.codex/
/.delivery/
/.worktrees/
/docs/
/work-items/
/AGENTS.md
/CLAUDE.md
/CHANGELOG.md

# Cargo.lock is committed because the workspace builds a binary, so every build resolves the same
# dependency versions. Uncomment only if the project becomes a library that does not commit it.
# Cargo.lock

# Local environment overrides and secrets
.env
.env.*
!.env.example

# Local databases and runtime data
*.db
*.db-shm
*.db-wal
/.local/

# Coverage and profiling output
/coverage/
*.profraw
flamegraph.svg

# Tool output
/.cache/

# Operating-system metadata
.DS_Store
Thumbs.db

# Never add these here: .sqlx/ (committed offline query metadata), migrations/, contracts/.

# Linked Git worktrees at any depth
.worktrees/
