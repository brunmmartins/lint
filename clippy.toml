# `clippy::unwrap_used` is a workspace lint, but tests are where `unwrap` belongs. Exempt them here
# rather than scattering `#[allow]` through the test suite (K §12.1, §12.5).
allow-unwrap-in-tests = true
