# Use-case orchestration crate for lint (ADR-0002): ports, the LintFault taxonomy, run_lint, and the
# pure format_finding/decide_exit_code functions. Depends on lint-domain only.
[package]
name = "lint-application"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
lint-domain.workspace = true
