# The toolchain contributors, editors, and CI use. It is not the MSRV. The MSRV is `rust-version` in
# Cargo.toml, and exists only once the project promises one.
[toolchain]
channel = "1.98.0"
profile = "minimal"
components = ["clippy", "rustfmt"]
